cd Downloads

ffmpeg -i input.mp4 -vf "fps=15,scale=1280:-1:flags=lanczos,palettegen" -y -update 1 palette.png

ffmpeg -i input.mp4 -i palette.png -filter_complex "fps=15,scale=1280:-1:flags=lanczos[x];[x][1:v]paletteuse" -y output.gif
